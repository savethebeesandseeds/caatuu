diff --git a/common/chat.cpp b/common/chat.cpp
index 6da59f4..8344e0b 100644
--- a/common/chat.cpp
+++ b/common/chat.cpp
@@ -608,11 +608,15 @@ std::string common_chat_format_single(const struct common_chat_templates * tmpls
                                       const std::vector<common_chat_msg> & past_msg,
                                       const common_chat_msg &              new_msg,
                                       bool                                 add_ass,
-                                      bool                                 use_jinja) {
+                                      bool                                 use_jinja,
+                                      bool                                 enable_thinking,
+                                      const std::map<std::string, std::string> & chat_template_kwargs) {
     common_chat_templates_inputs inputs;
-    inputs.use_jinja = use_jinja;
-    inputs.add_bos   = tmpls->add_bos;
-    inputs.add_eos   = tmpls->add_eos;
+    inputs.use_jinja            = use_jinja;
+    inputs.add_bos              = tmpls->add_bos;
+    inputs.add_eos              = tmpls->add_eos;
+    inputs.enable_thinking      = enable_thinking;
+    inputs.chat_template_kwargs = chat_template_kwargs;

     std::string fmt_past_msg;
     if (!past_msg.empty()) {
diff --git a/common/chat.h b/common/chat.h
index 7898f16..f7b0e16 100644
--- a/common/chat.h
+++ b/common/chat.h
@@ -329,7 +329,9 @@ std::string common_chat_format_single(const struct common_chat_templates * tmpls
                                       const std::vector<common_chat_msg> & past_msg,
                                       const common_chat_msg &              new_msg,
                                       bool                                 add_ass,
-                                      bool                                 use_jinja);
+                                      bool                                 use_jinja,
+                                      bool                                 enable_thinking = true,
+                                      const std::map<std::string, std::string> & chat_template_kwargs = {});

 // Returns an example of formatted chat
 std::string common_chat_format_example(const struct common_chat_templates *       tmpls,
diff --git a/examples/llama.android/lib/src/main/cpp/ai_chat.cpp b/examples/llama.android/lib/src/main/cpp/ai_chat.cpp
index 03ab96c..f46d505 100644
--- a/examples/llama.android/lib/src/main/cpp/ai_chat.cpp
+++ b/examples/llama.android/lib/src/main/cpp/ai_chat.cpp
@@ -2,6 +2,7 @@
 #include <jni.h>
 #include <iomanip>
 #include <cmath>
+#include <map>
 #include <string>
 #include <unistd.h>
 #include <sampling.h>
@@ -286,12 +287,16 @@ static void shift_context() {
     LOGi("%s: Context shifting done! Current position: %d", __func__, current_position);
 }

-static std::string chat_add_and_format(const std::string &role, const std::string &content) {
+static std::string chat_add_and_format(const std::string &role, const std::string &content,
+                                       const bool enable_thinking = true) {
     common_chat_msg new_msg;
     new_msg.role = role;
     new_msg.content = content;
+    std::map<std::string, std::string> template_kwargs;
+    template_kwargs["enable_thinking"] = enable_thinking ? "true" : "false";
     auto formatted = common_chat_format_single(
-            g_chat_templates.get(), chat_msgs, new_msg, role == ROLE_USER, /* use_jinja */ false);
+            g_chat_templates.get(), chat_msgs, new_msg, role == ROLE_USER, /* use_jinja */ true,
+            enable_thinking, template_kwargs);
     chat_msgs.push_back(new_msg);
     LOGi("%s: Formatted and added %s message: \n%s\n", __func__, role.c_str(), formatted.c_str());
     return formatted;
@@ -310,6 +315,14 @@ static void reset_short_term_states() {
     assistant_ss.str("");
 }

+static void reset_conversation_states() {
+    reset_long_term_states();
+    reset_short_term_states();
+    if (g_sampler != nullptr) {
+        common_sampler_reset(g_sampler);
+    }
+}
+
 static int decode_tokens_in_batches(
         llama_context *context,
         llama_batch &batch,
@@ -350,6 +363,15 @@ static int decode_tokens_in_batches(
     return 0;
 }

+extern "C"
+JNIEXPORT void JNICALL
+Java_com_arm_aichat_internal_InferenceEngineImpl_resetConversationNative(
+        JNIEnv * /*unused*/,
+        jobject /*unused*/
+) {
+    reset_conversation_states();
+}
+
 extern "C"
 JNIEXPORT jint JNICALL
 Java_com_arm_aichat_internal_InferenceEngineImpl_processSystemPrompt(
@@ -359,7 +381,6 @@ Java_com_arm_aichat_internal_InferenceEngineImpl_processSystemPrompt(
 ) {
     // Reset long-term & short-term states
-    reset_long_term_states();
-    reset_short_term_states();
+    reset_conversation_states();

     // Obtain system prompt from JEnv
     const auto *system_prompt = env->GetStringUTFChars(jsystem_prompt, nullptr);
@@ -405,7 +426,8 @@ Java_com_arm_aichat_internal_InferenceEngineImpl_processUserPrompt(
         JNIEnv *env,
         jobject /*unused*/,
         jstring juser_prompt,
-        jint n_predict
+        jint n_predict,
+        jboolean jenable_thinking
 ) {
     // Reset short-term states
     reset_short_term_states();
@@ -418,7 +440,8 @@ Java_com_arm_aichat_internal_InferenceEngineImpl_processUserPrompt(
     // Format user prompt if applicable
     const bool has_chat_template = common_chat_templates_was_explicit(g_chat_templates.get());
     if (has_chat_template) {
-        formatted_user_prompt = chat_add_and_format(ROLE_USER, user_prompt);
+        const bool enable_thinking = jenable_thinking == JNI_TRUE;
+        formatted_user_prompt = chat_add_and_format(ROLE_USER, user_prompt, enable_thinking);
     }
     env->ReleaseStringUTFChars(juser_prompt, user_prompt);

@@ -429,22 +452,22 @@ Java_com_arm_aichat_internal_InferenceEngineImpl_processUserPrompt(
     }

     // Ensure user prompt doesn't exceed the context size by truncating if necessary.
-    const int user_prompt_size = (int) user_tokens.size();
     const int max_batch_size = DEFAULT_CONTEXT_SIZE - OVERFLOW_HEADROOM;
-    if (user_prompt_size > max_batch_size) {
-        const int skipped_tokens = user_prompt_size - max_batch_size;
+    if ((int) user_tokens.size() > max_batch_size) {
+        const int skipped_tokens = (int) user_tokens.size() - max_batch_size;
         user_tokens.resize(max_batch_size);
         LOGw("%s: User prompt too long! Skipped %d tokens!", __func__, skipped_tokens);
     }
+    const int user_prompt_size = (int) user_tokens.size();

     // Decode user tokens in batches
     if (decode_tokens_in_batches(g_context, g_batch, user_tokens, current_position, true)) {
         LOGe("%s: llama_decode() failed!", __func__);
         return 2;
     }

     // Update position
     current_position += user_prompt_size;
-    stop_generation_position = current_position + user_prompt_size + n_predict;
+    stop_generation_position = current_position + n_predict;
     return 0;
 }
@@ -492,10 +515,14 @@ Java_com_arm_aichat_internal_InferenceEngineImpl_generateNextToken(
     // Infinite text generation via context shifting
     if (current_position >= DEFAULT_CONTEXT_SIZE - OVERFLOW_HEADROOM) {
         LOGw("%s: Context full! Shifting...", __func__);
+        const llama_pos remaining_generation = stop_generation_position > current_position
+                ? stop_generation_position - current_position
+                : 0;
         shift_context();
+        stop_generation_position = current_position + remaining_generation;
     }

     // Stop if reaching the marked position
     if (current_position >= stop_generation_position) {
         LOGw("%s: STOP: hitting stop position: %d", __func__, stop_generation_position);
         return nullptr;
     }
diff --git a/examples/llama.android/lib/src/main/java/com/arm/aichat/InferenceEngine.kt b/examples/llama.android/lib/src/main/java/com/arm/aichat/InferenceEngine.kt
index 26c1668..61138ec 100644
--- a/examples/llama.android/lib/src/main/java/com/arm/aichat/InferenceEngine.kt
+++ b/examples/llama.android/lib/src/main/java/com/arm/aichat/InferenceEngine.kt
@@ -23,12 +23,21 @@ interface InferenceEngine {
     /**
      * Sends a system prompt to the loaded model
      */
     suspend fun setSystemPrompt(systemPrompt: String)

+    /**
+     * Clears conversation history and KV cache while keeping the model loaded.
+     */
+    suspend fun resetConversation()
+
     /**
      * Sends a user prompt to the loaded model and returns a Flow of generated tokens.
      */
-    fun sendUserPrompt(message: String, predictLength: Int = DEFAULT_PREDICT_LENGTH): Flow<String>
+    fun sendUserPrompt(
+        message: String,
+        predictLength: Int = DEFAULT_PREDICT_LENGTH,
+        enableThinking: Boolean = true,
+    ): Flow<String>

     /**
      * Runs a benchmark with the specified parameters.
diff --git a/examples/llama.android/lib/src/main/java/com/arm/aichat/internal/InferenceEngineImpl.kt b/examples/llama.android/lib/src/main/java/com/arm/aichat/internal/InferenceEngineImpl.kt
index a293796..9c07aae 100644
--- a/examples/llama.android/lib/src/main/java/com/arm/aichat/internal/InferenceEngineImpl.kt
+++ b/examples/llama.android/lib/src/main/java/com/arm/aichat/internal/InferenceEngineImpl.kt
@@ -97,8 +97,15 @@ internal class InferenceEngineImpl private constructor(
     @FastNative
     private external fun processSystemPrompt(systemPrompt: String): Int

+    @FastNative
+    private external fun resetConversationNative()
+
     @FastNative
-    private external fun processUserPrompt(userPrompt: String, predictLength: Int): Int
+    private external fun processUserPrompt(
+        userPrompt: String,
+        predictLength: Int,
+        enableThinking: Boolean,
+    ): Int

     @FastNative
     private external fun generateNextToken(): String?
@@ -210,6 +217,19 @@ internal class InferenceEngineImpl private constructor(
             _state.value = InferenceEngine.State.ModelReady
         }

+    override suspend fun resetConversation() =
+        withContext(llamaDispatcher) {
+            check(_state.value is InferenceEngine.State.ModelReady) {
+                "Cannot reset conversation in ${_state.value.javaClass.simpleName}!"
+            }
+            Log.i(TAG, "Resetting conversation while keeping the model loaded...")
+            resetConversationNative()
+            _readyForSystemPrompt = true
+            _cancelGeneration = false
+            Log.i(TAG, "Conversation reset complete!")
+            Unit
+        }
+
     /**
      * Send plain text user prompt to LLM, which starts generating tokens in a [Flow]
      */
@@ -217,6 +237,7 @@ internal class InferenceEngineImpl private constructor(
     override fun sendUserPrompt(
         message: String,
         predictLength: Int,
+        enableThinking: Boolean,
     ): Flow<String> = flow {
         require(message.isNotEmpty()) { "User prompt discarded due to being empty!" }
         check(_state.value is InferenceEngine.State.ModelReady) {
@@ -228,10 +249,9 @@ internal class InferenceEngineImpl private constructor(
             _readyForSystemPrompt = false
             _state.value = InferenceEngine.State.ProcessingUserPrompt

-            processUserPrompt(message, predictLength).let { result ->
+            processUserPrompt(message, predictLength, enableThinking).let { result ->
                 if (result != 0) {
-                    Log.e(TAG, "Failed to process user prompt: $result")
-                    return@flow
+                    throw IOException("Failed to process user prompt: $result")
                 }
             }

@@ -310,15 +330,21 @@ internal class InferenceEngineImpl private constructor(
      * Cancel all ongoing coroutines and free GGML backends
      */
     override fun destroy() {
-        _cancelGeneration = true
-        runBlocking(llamaDispatcher) {
-            _readyForSystemPrompt = false
-            when(_state.value) {
-                is InferenceEngine.State.Uninitialized -> {}
-                is InferenceEngine.State.Initialized -> shutdown()
-                else -> { unload(); shutdown() }
+        try {
+            _cancelGeneration = true
+            runBlocking(llamaDispatcher) {
+                _readyForSystemPrompt = false
+                when(_state.value) {
+                    is InferenceEngine.State.Uninitialized -> {}
+                    is InferenceEngine.State.Initialized -> shutdown()
+                    else -> { unload(); shutdown() }
+                }
+            }
+            llamaScope.cancel()
+        } finally {
+            synchronized(Companion) {
+                if (instance === this@InferenceEngineImpl) instance = null
             }
         }
-        llamaScope.cancel()
     }
 }
