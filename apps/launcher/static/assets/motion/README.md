# Shared motion assets

This directory is the canonical catalog for reusable motion packages consumed
by Caatuu games. Each package owns its strict manifest, source binary, license,
provenance state, and promotion requirements.

A package may remain preview-only while provenance or distribution review is
incomplete. Central placement does not imply release approval.

